/**
 * @deprecated Legacy tab-based dashboard — superseded by react-router + Outlet.
 * The app now renders pages via /app/* routes in main.tsx.
 * Kept for reference only.
 */
export function Dashboard() {
  return <div>Legacy component. Use /app/* routes via react-router.</div>;
}
